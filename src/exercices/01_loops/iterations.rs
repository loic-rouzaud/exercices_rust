pub fn sum_even_numbers_like_c(start: i32, end: i32) -> i32 {
    // TODO() : Implémenter la fonction qui calcule la somme des nombres pairs
    // entre start et end (exclus) en utilisant une approche impérative (style C)
    // avec une boucle for et une accumulation manuelle
}

pub fn sum_even_numbers_like_rust(start: i32, end: i32) -> i32 {
    // TODO() : Implémenter la même fonction que précédemment, mais en utilisant
    // une approche fonctionnelle avec les méthodes d'itérateur (filter, sum)
}

fn sum_even_numbers1(numbers: &[i32]) -> i32 {
    // TODO() : Implémenter une fonction qui calcule la somme des nombres pairs
    // dans un tableau en utilisant les méthodes d'itérateur
}

pub fn process_strings(strings: Vec<&str>, min_lenght: usize) -> usize {
    // TODO() : Implémenter une fonction qui prend un vecteur de chaînes,
    // calcule la longueur de chaque chaîne, filtre celles qui dépassent
    // une longueur minimale, puis retourne la somme des longueurs restantes
}

pub fn square_roots_of_even(numbers: Vec<i32>) -> Vec<f64> {
    // TODO() : Implémenter une fonction qui filtre les nombres pairs
    // d'un vecteur, calcule leur racine carrée, et retourne les résultats
    // dans un nouveau vecteur
}

pub fn unique_words(sentences: Vec<&str>) -> Vec<String> {
    // TODO() : Implémenter une fonction qui extrait tous les mots uniques
    // d'un ensemble de phrases, les convertit en minuscules, les trie
    // et élimine les doublons
}

pub fn to_uppercase(strings: &[String]) -> Vec<String> {
    // TODO() : Implémenter une fonction qui convertit toutes les chaînes
    // d'un tableau en majuscules et retourne un nouveau vecteur
}

fn sum_until(limit: i32) -> i32 {
    // TODO() : Implémenter une fonction qui utilise une boucle 'while' pour
    // calculer la somme des nombres de 1 jusqu'à ce que cette somme dépasse 'limit'.
    // La fonction doit retourner le dernier nombre ajouté avant de dépasser la limite.
    // Exemple: si limit est 10, on calcule 1+2+3+4+5=15 (qui dépasse 10),
    // donc on retourne 5. Si limit est négatif, retourner 0.
    // Placeholder return value
}

fn factorial(n: u32) -> u64 {
    // TODO() : Implémenter une fonction qui calcule la factorielle de n (n!)
    // en utilisant une boucle 'for'. La factorielle est définie comme
    // n! = n × (n-1) × (n-2) × ... × 2 × 1
    // Par convention, 0! = 1
    // Placeholder return value
}

fn main() {
    // pour tester vos fonctions
}
